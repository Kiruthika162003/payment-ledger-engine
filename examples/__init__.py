"""Runnable end-to-end transcripts of the ledger in use.

Each example builds a small but complete scenario with the public
API and prints a deterministic transcript, so it doubles as
documentation and as a test: tests/test_examples.py pins every
line. Run one with python -m examples.<name>.
"""

from __future__ import annotations
